# StarSkill Evaluation v5 Final Re-review

## Findings

### Important

1. **The published reviewer rotation is the inverse of the replay and aggregate contract.**

   The public protocol says that a teacher reviewer reviews outreach Worker output, an outreach reviewer reviews research Worker output, and a research reviewer reviews teacher Worker output at `F:\Skill-for-stars\evaluation\README.md:18`, `F:\Skill-for-stars\evaluation\README.md:19`, and `F:\Skill-for-stars\evaluation\README.md:20`. The three reviewer prompts repeat those exact assignments at `F:\Skill-for-stars\evaluation\prompts\reviewers\teacher-review.md:4`, `F:\Skill-for-stars\evaluation\prompts\reviewers\outreach-review.md:4`, and `F:\Skill-for-stars\evaluation\prompts\reviewers\research-review.md:4`. Replay instead requires teacher case -> outreach reviewer, outreach case -> research reviewer, and research case -> teacher reviewer at `F:\Skill-for-stars\scripts\evaluate_starskill.py:234`; aggregate enforces the same mapping at `F:\Skill-for-stars\src\starskill\evaluation\reporting.py:383`. A fresh full-replay diagnostic for the teacher-role `core-m42-beijing` case rejected the documented research reviewer with structured `invalid_reviewer_rotation` but accepted an outreach reviewer, whose own prompt says it must review only research Worker output. The documentation test at `F:\Skill-for-stars\tests\test_evaluation_cases.py:233` checks that the README and prompts agree with each other, but never compares that mapping with either validator. An external harness cannot follow the published reviewer prompts and also satisfy replay/aggregate, and adjudicator escalation inherits the same incompatible expected normal-review role.

### Minor

2. **Oversized non-bonus JSON inputs still escape the structured CLI error boundary.**

   The v5 top-level bonus parser correctly catches plain `ValueError` at `F:\Skill-for-stars\scripts\evaluate_starskill.py:202`, and referenced bonus records do the same at `F:\Skill-for-stars\src\starskill\evaluation\reporting.py:503`. Other CLI JSON boundaries still catch only `JSONDecodeError`: reviewer JSON at `F:\Skill-for-stars\scripts\evaluate_starskill.py:185`, escalation JSON at `F:\Skill-for-stars\scripts\evaluate_starskill.py:218`, case manifests at `F:\Skill-for-stars\src\starskill\evaluation\cases.py:88`, and aggregate score bundles at `F:\Skill-for-stars\src\starskill\evaluation\reporting.py:608`. Since `main()` catches only `CaseManifestError` and `ReportError` at `F:\Skill-for-stars\scripts\evaluate_starskill.py:48`, CPython's integer-string digit-limit `ValueError` bypasses the documented JSON error response. Fresh isolated diagnostics supplied a 10,000-digit integer separately in a case manifest, reviewer file, and `score.json`; all three escaped `main()` as raw `ValueError`. This remains inconsistent with the malformed-input requirement at `F:\Skill-for-stars\docs\superpowers\fixes\2026-07-21-final-review-fix-brief.md:17`.

## Assumptions And Evidence

- `F:\Skill-for-stars` is the authoritative current filesystem and intentionally has no Git metadata. No source, manifest, test, fixture, prompt, or documentation file was edited; only this requested report was added.
- The controller's 97 focused and 191 full tests, `compileall`, `pip check`, and CLI-help results are treated as independently supplied evidence. Fresh local collection found 89 tests in the seven listed evaluator files; that focused suite exited `0`. The count difference may reflect controller-owned tests and is not itself treated as a product finding.
- Aggregate identity now binds `run_id` to the resolved run-directory name at `F:\Skill-for-stars\src\starskill\evaluation\reporting.py:652` and rejects duplicate physical `(case_id, resolved_run_dir)` entries at `F:\Skill-for-stars\src\starskill\evaluation\reporting.py:154`. Copied score bundles pointing to one physical run cannot satisfy the three-repeat matrix, while equal directory basenames under distinct physical parents remain allowed.
- Top-level and referenced oversized bonus JSON now return structured errors through `F:\Skill-for-stars\scripts\evaluate_starskill.py:199` and `F:\Skill-for-stars\src\starskill\evaluation\reporting.py:502`. Finding 2 concerns the remaining non-bonus JSON loaders.
- The published 14-key `tool_calls.jsonl` schema matches `_EXECUTION_RECORD_FIELDS` at `F:\Skill-for-stars\src\starskill\evaluation\reporting.py:41`, the exact-key validator at `F:\Skill-for-stars\src\starskill\evaluation\reporting.py:332`, the linked nested-result validation at `F:\Skill-for-stars\src\starskill\evaluation\reporting.py:346`, and the Worker/CLI documentation. Wrong case, role, task, workflow, run/output directory, return code, evidence path, or nested result fails closed.
- Finite scientific evidence is enforced for JSON at `F:\Skill-for-stars\src\starskill\evaluation\checks.py:255` and CSV at `F:\Skill-for-stars\src\starskill\evaluation\checks.py:346`; the focused suite exercised `NaN`, `Infinity`, and `-Infinity` rejection.
- Canonical case loading is project-anchored at `F:\Skill-for-stars\scripts\evaluate_starskill.py:133`; current manifests have unique IDs; aggregate rederives machine, review, bonus, and score evidence before enforcing the three-core/six-variant matrix at `F:\Skill-for-stars\src\starskill\evaluation\reporting.py:580`.
- Worker-role binding and adjudicator evidence checks fail closed in source at `F:\Skill-for-stars\scripts\evaluate_starskill.py:227` and `F:\Skill-for-stars\src\starskill\evaluation\reporting.py:401`. Finding 1 is the public normal-review routing mismatch, not a bypass of those checks.
- Bonus claims require linked, readable, non-empty strict measurement and passing-verification records at `F:\Skill-for-stars\src\starskill\evaluation\reporting.py:434` through `F:\Skill-for-stars\src\starskill\evaluation\reporting.py:477`; bare prose, equal measurements, non-finite values, missing files, and escaping paths are rejected.
- M51 metadata normalization is restricted to the exact legacy `runs/day6_m51` prefix at `F:\Skill-for-stars\src\starskill\evaluation\checks.py:988`, and unrelated basenames remain rejected. The focused suite passed both real-shaped legacy-path and unrelated-path regressions.
- The Guangzhou task values `31.635` and `73.765` are present at `F:\Skill-for-stars\evaluation\cases\variants\variant-moon-jupiter-location-time.json:19`, match the task's Guangzhou coordinates/time, and the focused regression rejects the Shanghai values.
- Open tasks are scaled independently to 20 at `F:\Skill-for-stars\src\starskill\evaluation\scoring.py:115`, excluded from fixed/core decisions at `F:\Skill-for-stars\src\starskill\evaluation\scoring.py:155`, and rendered separately at `F:\Skill-for-stars\src\starskill\evaluation\reporting.py:812`.
- The external 9+ Agent evaluation remains unrun and is not inferred from repository tests.

## Verdict

**FAIL - changes required.** The v5 physical-run identity, oversized bonus handling, and execution-schema fixes are present, and the other named prior fail-open paths remain closed in source and bounded tests. The external reviewer protocol is still impossible to satisfy consistently because its role rotation is opposite the validators, and oversized case/review/score JSON can still escape the structured CLI contract.
