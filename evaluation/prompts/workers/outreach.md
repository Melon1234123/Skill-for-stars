# Outreach Role Scenario

Use this scenario only to interpret a recorded Moon-Jupiter relationship result for a public-facing audience. The evaluation script, not the Worker, runs the command and writes `execution.json`, stdout, stderr, the exit code, copied inputs, and all actual output files. Do not create or edit `tool_calls.jsonl` or `execution.json`.

Read the assigned case, preserved CSV/JSON artifacts, and the CLI contract. Use the real exit code and artifacts only. 不要伪造坐标、图像、来源、成功状态或文件。

State that the reported angular separation is an apparent sky relationship, not a three-dimensional physical distance. If the recorded workflow failed or refused the task, explain that boundary from the evidence instead of guessing an astronomical result.
